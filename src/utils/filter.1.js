import { FilterType } from '../const';

const filter = {
  [FilterType.EVERYTHING]: (points) => points.filter(points),
  [FilterType.FUTURE]: (points) => points.filter(points)
};
isPointFuture(point.travelDate),
  [FilterType.PRESENT];
(points) => points.filter(points);
isPointPresent(point.travelDate),
  [FilterType.PAST];
(points) => points.filter(points);
isPointPast(point.travelDate),
  [FilterType.FAVORITES];
(points) => points.filter((_points) => isPointFavorites),
;
;
export { filter };
